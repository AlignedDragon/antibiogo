- std interval is completely wrong.
- did i not confuse logsigma to sigma with estimations?
- train a little bit with mse for radius only (pushing std to zero) then go with gnll with very small lr
- make it converge somehow

- yolo is struggling with dishes - better have separate small obj det models.
- train yolo with augmented dataset
- test with slicer bcz there might me some noise in the center of yolo.

* MNv3_0.1_LRS is struggling with big radii -> ? go with polar coordinates with center jitter. 
* after 200 epochs - overfitting

? go with bigger model and distill
? train an ensemble only
? do i really need to work with logsigma? no, you can take sigma and homomorphic transform
? but why normalizing to [-1,1] would help so much?
? if it doesn't work simply clip the the probability from below
? stop and think what is happening at all?
? freeze some params

! xyr model output two values, [0] is the mean [1] is the standard deviation. all in [-1,1] range. they must be transformed back
! average radius of a pellet is 3 mm. mean = 16.6, min = 11.4 for pellet radius -> 1mm = 3.8 pixel


# xyr_dataset/train - 14774 
# yolo_dataset/train - 1140
# yolo is ->  Total params: 3,433,148 (13.10 MB)
